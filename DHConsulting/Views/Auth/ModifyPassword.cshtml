@{
    ViewBag.Title = "Nuova Password";
}

@section style{
    <style>
        .password-hero {
            background: linear-gradient(135deg, #0a0a0a 0%, #1f1f1f 50%, #2a2a2a 100%);
            position: relative;
            overflow: hidden;
        }

        .floating-particles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            background: rgba(251, 191, 36, 0.08);
            border-radius: 50%;
            animation: float 12s ease-in-out infinite;
        }

        .particle:nth-child(1) {
            width: 300px;
            height: 300px;
            top: 10%;
            left: 5%;
            animation-delay: 0s;
        }

        .particle:nth-child(2) {
            width: 180px;
            height: 180px;
            top: 50%;
            right: 10%;
            animation-delay: -5s;
        }

        .particle:nth-child(3) {
            width: 120px;
            height: 120px;
            bottom: 20%;
            left: 40%;
            animation-delay: -8s;
        }

        @@keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            33% { transform: translateY(-20px) rotate(1deg); }
            66% { transform: translateY(10px) rotate(-1deg); }
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .input-group {
            position: relative;
            margin-bottom: 1.5rem;
        }

        .floating-label {
            position: absolute;
            left: 1rem;
            top: 1rem;
            color: #6b7280;
            transition: all 0.3s ease;
            pointer-events: none;
            background: white;
            padding: 0 0.5rem;
            font-size: 0.9rem;
        }

        .form-input:focus + .floating-label,
        .form-input:not(:placeholder-shown) + .floating-label {
            transform: translateY(-1.75rem) scale(0.85);
            color: #fbbf24;
            font-weight: 500;
        }

        .form-input {
            width: 100%;
            padding: 1rem;
            border: 2px solid #e5e7eb;
            border-radius: 1rem;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.95);
            font-size: 1rem;
        }

        .form-input:focus {
            outline: none;
            border-color: #fbbf24;
            box-shadow: 0 0 0 3px rgba(251, 191, 36, 0.1);
            transform: translateY(-1px);
        }

        .form-input.error {
            border-color: #ef4444;
            box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.1);
        }

        .form-input.success {
            border-color: #10b981;
            box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
        }

        .btn-primary-custom {
            background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
            border: none;
            color: #000;
            font-weight: 600;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-primary-custom:hover {
            background: linear-gradient(135deg, #fcd34d 0%, #fbbf24 100%);
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(251, 191, 36, 0.4);
        }

        .btn-primary-custom:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .password-toggle {
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #6b7280;
            transition: color 0.3s ease;
            z-index: 10;
        }

        .password-toggle:hover {
            color: #fbbf24;
        }

        .alert-custom {
            border-radius: 1rem;
            animation: slideIn 0.3s ease-out;
        }

        .alert-error {
            background: linear-gradient(135deg, #fef2f2 0%, #fef7f7 100%);
            border: 1px solid #fca5a5;
        }

        .alert-success {
            background: linear-gradient(135deg, #f0fdf4 0%, #f7fef9 100%);
            border: 1px solid #86efac;
        }

        @@keyframes slideIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .brand-section {
            color: white;
        }

        .feature-item {
            display: flex;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .feature-icon {
            width: 3rem;
            height: 3rem;
            background: rgba(251, 191, 36, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
            border: 2px solid rgba(251, 191, 36, 0.3);
            flex-shrink: 0;
        }

        .password-strength {
            margin-top: 1rem;
        }

        .strength-bar {
            height: 4px;
            background: #e5e7eb;
            border-radius: 2px;
            overflow: hidden;
            margin-bottom: 0.5rem;
        }

        .strength-fill {
            height: 100%;
            border-radius: 2px;
            transition: all 0.3s ease;
            width: 0%;
        }

        .strength-weak { background: #ef4444; }
        .strength-fair { background: #f97316; }
        .strength-good { background: #eab308; }
        .strength-strong { background: #10b981; }

        .password-requirements {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            border: 1px solid #e2e8f0;
            border-radius: 1rem;
            padding: 1rem;
            margin-top: 1rem;
        }

        .requirement {
            display: flex;
            align-items: center;
            margin-bottom: 0.5rem;
            font-size: 0.875rem;
            transition: all 0.3s ease;
        }

        .requirement.met {
            color: #10b981;
        }

        .requirement.not-met {
            color: #6b7280;
        }

        .requirement-icon {
            margin-right: 0.5rem;
            width: 16px;
            text-align: center;
        }

        .security-tips {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 1rem;
            padding: 1.5rem;
            margin-top: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .tip-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 1rem;
            padding: 0.75rem;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 0.5rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .tip-icon {
            width: 1.5rem;
            height: 1.5rem;
            background: rgba(251, 191, 36, 0.3);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 0.75rem;
            flex-shrink: 0;
            margin-top: 0.125rem;
        }

        @@media (max-width: 1024px) {
            .password-hero {
                min-height: auto;
                padding: 2rem 0;
            }

            .glass-card {
                margin: 1rem;
            }
        }
    </style>
}

<main class="password-hero min-h-screen">
    <!-- Floating Particles -->
    <div class="floating-particles">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
    </div>

    <div class="relative min-h-screen flex items-center justify-center p-4">
        <div class="w-full max-w-6xl mx-auto grid lg:grid-cols-2 gap-12 items-center">

            <!-- Left Side - Branding & Security Tips -->
            <div class="brand-section hidden lg:block" data-aos="fade-right" data-aos-duration="1000">
                <div class="max-w-lg">
                    <!-- Logo -->
                    <div class="mb-8">
                        <img src="~/Content/Img/logo_orizz_sf.png" alt="Five Innovation Hub" class="w-80 mb-6">
                    </div>

                    <h1 class="text-5xl font-bold mb-6 leading-tight">
                        Imposta una nuova
                        <span class="bg-gradient-to-r from-yellow-400 to-yellow-600 bg-clip-text text-transparent font-caveat text-6xl block">
                            password sicura
                        </span>
                    </h1>

                    <p class="text-xl text-gray-300 mb-8 leading-relaxed">
                        Scegli una password forte e sicura per proteggere il tuo account.
                        Segui i nostri consigli per una sicurezza ottimale.
                    </p>

                    <!-- Security Features -->
                    <div class="space-y-6">
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fa-solid fa-shield-halved text-yellow-400 text-lg"></i>
                            </div>
                            <div>
                                <h3 class="font-semibold text-lg">Crittografia Avanzata</h3>
                                <p class="text-gray-400">La tua password è protetta con hash</p>
                            </div>
                        </div>

                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fa-solid fa-eye-slash text-yellow-400 text-lg"></i>
                            </div>
                            <div>
                                <h3 class="font-semibold text-lg">Privacy Totale</h3>
                                <p class="text-gray-400">Nessuno può vedere la tua password, nemmeno noi</p>
                            </div>
                        </div>

                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fa-solid fa-clock text-yellow-400 text-lg"></i>
                            </div>
                            <div>
                                <h3 class="font-semibold text-lg">Aggiornamento Immediato</h3>
                                <p class="text-gray-400">La nuova password sarà attiva subito</p>
                            </div>
                        </div>
                    </div>

                    <!-- Security Tips -->
                    <div class="security-tips">
                        <h4 class="font-semibold text-lg mb-4 text-yellow-400">Consigli per la sicurezza:</h4>
                        <div class="space-y-3">
                            <div class="tip-item">
                                <div class="tip-icon">
                                    <i class="fa-solid fa-lightbulb text-yellow-400 text-xs"></i>
                                </div>
                                <div class="text-sm">
                                    <div class="font-medium">Usa una combinazione unica</div>
                                    <div class="text-gray-400">Lettere maiuscole, minuscole, numeri e simboli</div>
                                </div>
                            </div>
                            <div class="tip-item">
                                <div class="tip-icon">
                                    <i class="fa-solid fa-ban text-yellow-400 text-xs"></i>
                                </div>
                                <div class="text-sm">
                                    <div class="font-medium">Evita informazioni personali</div>
                                    <div class="text-gray-400">Non usare nome, date di nascita o parole comuni</div>
                                </div>
                            </div>
                            <div class="tip-item">
                                <div class="tip-icon">
                                    <i class="fa-solid fa-refresh text-yellow-400 text-xs"></i>
                                </div>
                                <div class="text-sm">
                                    <div class="font-medium">Cambia regolarmente</div>
                                    <div class="text-gray-400">Aggiorna la password ogni 3-6 mesi</div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Right Side - Password Form -->
            <div class="w-full" data-aos="fade-left" data-aos-duration="1000" data-aos-delay="200">
                <div class="glass-card rounded-3xl shadow-2xl p-8 lg:p-12">

                    <!-- Mobile Logo -->
                    <div class="lg:hidden text-center mb-8">
                        <img src="/Content/Img/logo_orizz_sf.png" alt="Five Innovation Hub" class="w-60 mx-auto mb-4">
                        <h2 class="text-2xl font-bold text-gray-900">Nuova Password</h2>
                    </div>

                    <!-- Desktop Header -->
                    <div class="hidden lg:block text-center mb-8">
                        <div class="w-20 h-20 bg-gradient-to-r from-yellow-400 to-yellow-600 rounded-full flex items-center justify-center mx-auto mb-6 shadow-lg">
                            <i class="fa-solid fa-lock text-3xl text-white"></i>
                        </div>
                        <h2 class="text-3xl font-bold text-gray-900 mb-2">Imposta nuova password</h2>
                        <p class="text-gray-600">Crea una password sicura per proteggere il tuo account</p>
                    </div>

                    <!-- Error Messages -->
                    @if (ViewBag.Password != null)
                    {
                        <div class="alert-custom alert-error p-4 mb-6">
                            <div class="flex items-center">
                                <div class="flex items-center justify-center w-10 h-10 bg-red-100 rounded-full mr-3">
                                    <i class="fa-solid fa-exclamation-triangle text-red-600"></i>
                                </div>
                                <div>
                                    <h4 class="font-semibold text-red-800">Attenzione</h4>
                                    <p class="text-red-700 text-sm">@ViewBag.Password</p>
                                </div>
                            </div>
                        </div>
                    }

                    <!-- Password Form -->
                    @using (Html.BeginForm("ModifyPassword", "Auth", FormMethod.Post, new { @class = "space-y-6", @id = "passwordForm" }))
                    {
                        @Html.AntiForgeryToken()

                        <!-- Password Field -->
                        <div class="input-group">
                            <div class="relative">
                                <input type="password"
                                       name="NewPassword"
                                       id="newPasswordField"
                                       class="form-input pr-12"
                                       placeholder=" "
                                       autocomplete="new-password"
                                       required />
                                <label class="floating-label">
                                    <i class="fa-solid fa-lock mr-2"></i>Nuova Password
                                </label>
                                <span class="password-toggle" onclick="togglePassword('newPasswordField', 'passwordIcon1')">
                                    <i class="fa-solid fa-eye" id="passwordIcon1"></i>
                                </span>
                            </div>

                            <!-- Password Strength Indicator -->
                            <div class="password-strength">
                                <div class="strength-bar">
                                    <div class="strength-fill" id="strengthBar"></div>
                                </div>
                                <div class="flex justify-between items-center">
                                    <span class="text-sm text-gray-600">Forza password:</span>
                                    <span class="text-sm font-medium" id="strengthText">Inserisci una password</span>
                                </div>
                            </div>
                        </div>

                        <!-- Confirm Password Field -->
                        <div class="input-group">
                            <div class="relative">
                                <input type="password"
                                       name="ConfirmPassword"
                                       id="confirmPasswordField"
                                       class="form-input pr-12"
                                       placeholder=" "
                                       autocomplete="new-password"
                                       required />
                                <label class="floating-label">
                                    <i class="fa-solid fa-lock mr-2"></i>Conferma Password
                                </label>
                                <span class="password-toggle" onclick="togglePassword('confirmPasswordField', 'passwordIcon2')">
                                    <i class="fa-solid fa-eye" id="passwordIcon2"></i>
                                </span>
                            </div>
                            <div id="passwordMatchMessage" class="text-sm mt-1"></div>
                        </div>

                        <!-- Password Requirements -->
                        <div class="password-requirements">
                            <h4 class="font-semibold text-gray-800 mb-3">La password deve contenere:</h4>
                            <div class="space-y-2">
                                <div class="requirement not-met" id="req-length">
                                    <span class="requirement-icon"><i class="fa-solid fa-circle-xmark"></i></span>
                                    Almeno 8 caratteri
                                </div>
                                <div class="requirement not-met" id="req-lowercase">
                                    <span class="requirement-icon"><i class="fa-solid fa-circle-xmark"></i></span>
                                    Una lettera minuscola
                                </div>
                                <div class="requirement not-met" id="req-uppercase">
                                    <span class="requirement-icon"><i class="fa-solid fa-circle-xmark"></i></span>
                                    Una lettera maiuscola
                                </div>
                                <div class="requirement not-met" id="req-number">
                                    <span class="requirement-icon"><i class="fa-solid fa-circle-xmark"></i></span>
                                    Un numero
                                </div>
                                <div class="requirement not-met" id="req-special">
                                    <span class="requirement-icon"><i class="fa-solid fa-circle-xmark"></i></span>
                                    Un carattere speciale (.!?@@&$%)
                                </div>
                            </div>
                        </div>

                        <!-- Submit Button -->
                        <button type="submit" class="btn btn-primary-custom w-full btn-lg rounded-xl" id="submitBtn" disabled>
                            <i class="fa-solid fa-check mr-2"></i>
                            Aggiorna Password
                        </button>
                    }

                    <!-- Footer Links -->
                    <div class="text-center mt-8">
                        <span class="text-sm text-gray-600">Ricordi la tua password? </span>
                        <a href="@Url.Action("Login", "Auth")"
                           class="text-sm text-yellow-600 hover:text-yellow-700 font-semibold transition-colors">
                            <i class="fa-solid fa-sign-in-alt mr-1"></i>
                            Torna al Login
                        </a>
                    </div>

                    <!-- Security Notice -->
                    <div class="mt-6 p-4 bg-green-50 border border-green-200 rounded-xl">
                        <div class="flex items-center">
                            <div class="flex items-center justify-center w-8 h-8 bg-green-100 rounded-full mr-3">
                                <i class="fa-solid fa-shield-check text-green-600 text-sm"></i>
                            </div>
                            <div>
                                <h4 class="font-semibold text-green-800 text-sm">Connessione Sicura</h4>
                                <p class="text-green-700 text-xs">
                                    I tuoi dati sono protetti con crittografia SSL a 256 bit
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

@section scripts {
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        // Initialize AOS
        AOS.init({
            duration: 1000,
            once: true,
            offset: 100
        });

        // Password validation variables
        let passwordRequirements = {
            length: false,
            lowercase: false,
            uppercase: false,
            number: false,
            special: false
        };

        // Password Toggle
        function togglePassword(fieldId, iconId) {
            const passwordField = document.getElementById(fieldId);
            const passwordIcon = document.getElementById(iconId);

            if (passwordField.type === 'password') {
                passwordField.type = 'text';
                passwordIcon.className = 'fa-solid fa-eye-slash';
            } else {
                passwordField.type = 'password';
                passwordIcon.className = 'fa-solid fa-eye';
            }
        }

        // Password Strength Calculator
        function calculatePasswordStrength(password) {
            let score = 0;

            // Length
            if (password.length >= 8) score++;
            if (password.length >= 12) score++;

            // Character types
            if (/[a-z]/.test(password)) score++;
            if (/[A-Z]/.test(password)) score++;
            if (/[0-9]/.test(password)) score++;
            if (/[.!?@@&$%]/.test(password)) score++;

            // Bonus for variety
            if (password.length >= 10 && score >= 4) score++;

            return score;
        }

        // Update Password Strength Indicator
        function updatePasswordStrength(password) {
            const strengthBar = document.getElementById('strengthBar');
            const strengthText = document.getElementById('strengthText');
            const score = calculatePasswordStrength(password);

            let width, className, text;

            switch (true) {
                case score <= 2:
                    width = '25%';
                    className = 'strength-weak';
                    text = 'Debole';
                    break;
                case score <= 4:
                    width = '50%';
                    className = 'strength-fair';
                    text = 'Discreta';
                    break;
                case score <= 5:
                    width = '75%';
                    className = 'strength-good';
                    text = 'Buona';
                    break;
                default:
                    width = '100%';
                    className = 'strength-strong';
                    text = 'Forte';
            }

            strengthBar.style.width = width;
            strengthBar.className = `strength-fill ${className}`;
            strengthText.textContent = text;
        }

        // Validate Password Requirements
        function validatePasswordRequirements(password) {
            const requirements = {
                length: password.length >= 8,
                lowercase: /[a-z]/.test(password),
                uppercase: /[A-Z]/.test(password),
                number: /[0-9]/.test(password),
                special: /[.!?@@&$%]/.test(password)
            };

            // Update requirement indicators
            Object.keys(requirements).forEach(req => {
                const element = document.getElementById(`req-${req}`);
                const icon = element.querySelector('.requirement-icon i');

                if (requirements[req]) {
                    element.className = 'requirement met';
                    icon.className = 'fa-solid fa-circle-check';
                } else {
                    element.className = 'requirement not-met';
                    icon.className = 'fa-solid fa-circle-xmark';
                }
            });

            passwordRequirements = requirements;
            updateSubmitButton();
        }

        // Check Password Match
        function checkPasswordMatch() {
            const newPassword = document.getElementById('newPasswordField').value;
            const confirmPassword = document.getElementById('confirmPasswordField').value;
            const messageElement = document.getElementById('passwordMatchMessage');
            const confirmField = document.getElementById('confirmPasswordField');

            if (confirmPassword === '') {
                messageElement.textContent = '';
                confirmField.classList.remove('error', 'success');
                return false;
            }

            if (newPassword === confirmPassword) {
                messageElement.textContent = '✓ Le password corrispondono';
                messageElement.className = 'text-sm mt-1 text-green-600';
                confirmField.classList.remove('error');
                confirmField.classList.add('success');
                return true;
            } else {
                messageElement.textContent = '✗ Le password non corrispondono';
                messageElement.className = 'text-sm mt-1 text-red-600';
                confirmField.classList.remove('success');
                confirmField.classList.add('error');
                return false;
            }
        }

        // Update Submit Button State
        function updateSubmitButton() {
            const submitBtn = document.getElementById('submitBtn');
            const allRequirementsMet = Object.values(passwordRequirements).every(req => req);
            const passwordsMatch = checkPasswordMatch();
            const newPassword = document.getElementById('newPasswordField').value;

            if (allRequirementsMet && passwordsMatch && newPassword.length > 0) {
                submitBtn.disabled = false;
                submitBtn.classList.remove('opacity-60');
            } else {
                submitBtn.disabled = true;
                submitBtn.classList.add('opacity-60');
            }
        }

        // Form Enhancement
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('passwordForm');
            const newPasswordField = document.getElementById('newPasswordField');
            const confirmPasswordField = document.getElementById('confirmPasswordField');
            const submitBtn = document.getElementById('submitBtn');

            // Password input validation
            newPasswordField.addEventListener('input', function() {
                const password = this.value;
                updatePasswordStrength(password);
                validatePasswordRequirements(password);
                if (confirmPasswordField.value) {
                    checkPasswordMatch();
                }
            });

            // Confirm password validation
            confirmPasswordField.addEventListener('input', function() {
                checkPasswordMatch();
                updateSubmitButton();
            });

            // Form submission
            form.addEventListener('submit', function(e) {
                const allRequirementsMet = Object.values(passwordRequirements).every(req => req);
                const passwordsMatch = checkPasswordMatch();

                if (!allRequirementsMet || !passwordsMatch) {
                    e.preventDefault();
                    showAlert('Assicurati che la password rispetti tutti i requisiti e che le password corrispondano.', 'error');
                    return false;
                }

                // Show loading state
                submitBtn.disabled = true;
                submitBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin mr-2"></i>Aggiornando...';
            });

            // Add ripple effect to button
            submitBtn.addEventListener('click', function(e) {
                if (this.disabled) return;

                const ripple = document.createElement('span');
                const rect = this.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                const x = e.clientX - rect.left - size / 2;
                const y = e.clientY - rect.top - size / 2;

                ripple.style.width = ripple.style.height = size + 'px';
                ripple.style.left = x + 'px';
                ripple.style.top = y + 'px';
                ripple.style.position = 'absolute';
                ripple.style.borderRadius = '50%';
                ripple.style.background = 'rgba(255, 255, 255, 0.6)';
                ripple.style.transform = 'scale(0)';
                ripple.style.animation = 'ripple 0.6s linear';
                ripple.style.pointerEvents = 'none';

                this.style.position = 'relative';
                this.style.overflow = 'hidden';
                this.appendChild(ripple);

                setTimeout(() => {
                    ripple.remove();
                }, 600);
            });

            // Enhanced input focus effects
            const inputs = document.querySelectorAll('.form-input');
            inputs.forEach(input => {
                input.addEventListener('focus', function() {
                    this.parentElement.style.transform = 'scale(1.01)';
                });

                input.addEventListener('blur', function() {
                    this.parentElement.style.transform = 'scale(1)';
                });
            });
        });

        function showAlert(message, type = 'info') {
            const alertDiv = document.createElement('div');
            alertDiv.className = `alert-custom ${type === 'error' ? 'alert-error' : 'alert-success'} p-4 mb-4`;
            alertDiv.innerHTML = `
                <div class="flex items-center">
                    <i class="fa-solid fa-exclamation-triangle ${type === 'error' ? 'text-red-600' : 'text-green-600'} mr-2"></i>
                    <span class="${type === 'error' ? 'text-red-800' : 'text-green-800'}">${message}</span>
                </div>
            `;

            const form = document.getElementById('passwordForm');
            form.insertBefore(alertDiv, form.firstChild);

            setTimeout(() => {
                alertDiv.remove();
            }, 5000);
        }
    </script>

    <style>
        @@keyframes ripple {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }
    </style>
}